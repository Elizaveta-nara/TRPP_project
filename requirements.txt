# Основные зависимости
Flask>=2.0.0
python-docx>=0.8.10
openpyxl>=3.0.0
cryptography>=3.4.0

# Для работы с датами (если используется в коде)
python-dateutil>=2.8.0

# Дополнительные зависимости (если используются)
matplotlib>=3.0.0  # Для генерации графиков
flask-wtf>=1.0.0   # Для форм с CSRF-защитой